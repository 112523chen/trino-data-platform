[project]
name = "trino-data-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.8",
    "dbt-score>=0.16.0",
    "dbt-trino>=1.10.1",
]
